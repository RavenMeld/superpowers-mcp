[project]
name = "awesome-skills-context-engine"
version = "0.1.0"
description = "Context-aware, local-first skill index + search for SKILL.md files."
requires-python = ">=3.11"
readme = "README.md"
license = { text = "UNLICENSED" }

[tool.ruff]
line-length = 100
