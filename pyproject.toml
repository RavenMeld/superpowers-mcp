[project]
name = "awesome-skills"
version = "0.1.0"
description = "Local-first database + search for SKILL.md files (condensed cards + SQLite FTS)."
requires-python = ">=3.11"
readme = "README.md"
license = { text = "UNLICENSED" }

[tool.ruff]
line-length = 100

